# 🚀 Развертывание на GitHub Pages

Этот репозиторий настроен для автоматического развертывания сайта из папки `wp-theme/local/` на GitHub Pages.

## 📋 Как это работает

1. При каждом push в ветку `main` запускается GitHub Actions workflow
2. Workflow конвертирует PHP файлы в статические HTML
3. Сгенерированные файлы автоматически публикуются на GitHub Pages

## ⚙️ Настройка GitHub Pages

### Первый запуск:

1. Перейдите в настройки репозитория: **Settings** → **Pages**
2. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
   - Это включит автоматическое развертывание через workflow

3. После первого push workflow автоматически создаст сайт
4. URL сайта будет: `https://dzhokhar1.github.io/edelweiss/`

## 📝 Доступные страницы

После деплоя будут доступны:
- `/` - Главная страница
- `/about.html` - О саде
- `/groups.html` - Возрастные группы
- `/programs.html` - Программы
- `/pricing.html` - Режим и стоимость
- `/adaptation.html` - Адаптация
- `/enrollment.html` - Как записаться
- `/contacts.html` - Контакты

## 🔄 Обновление сайта

Просто сделайте изменения в папке `wp-theme/local/` и запушите в репозиторий:

```bash
git add wp-theme/local/
git commit -m "Обновление сайта"
git push origin main
```

GitHub Actions автоматически пересоберет и опубликует сайт.

## 🛠️ Локальная генерация статики

Вы можете локально сгенерировать статические HTML файлы:

```bash
cd wp-theme/local
./generate-static.sh
```

Сгенерированные файлы будут в папке `wp-theme/gh-pages/`

## 📁 Структура

- `wp-theme/local/` - PHP версия сайта
- `wp-theme/gh-pages/` - Сгенерированные статические HTML файлы (создается автоматически)
- `.github/workflows/deploy-gh-pages.yml` - GitHub Actions workflow

## ⚠️ Важно

GitHub Pages поддерживает только статические HTML/CSS/JS, поэтому PHP код конвертируется в статический HTML при деплое.

