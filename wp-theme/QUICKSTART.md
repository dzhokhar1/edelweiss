# Быстрый старт - Edelweiss Garden WordPress Theme

## 🚀 Установка за 3 шага

### Шаг 1: Подготовка
```bash
# Убедитесь, что у вас есть WordPress установка
# Версия WordPress: 6.0+
# Версия PHP: 8.1+
```

### Шаг 2: Установка темы

**Вариант A - Через ZIP:**
1. Заархивируйте папку `edelweiss-garden` в ZIP файл
2. WordPress → Внешний вид → Темы → Добавить новую → Загрузить тему
3. Выберите ZIP файл

**Вариант B - Через FTP:**
```bash
# Скопируйте папку темы в WordPress
cp -r edelweiss-garden /path/to/wordpress/wp-content/themes/
```

### Шаг 3: Активация
1. WordPress → Внешний вид → Темы
2. Найдите "Edelweiss Garden"
3. Нажмите "Активировать"

## ✅ Готово!

Тема установлена и готова к использованию. Главная страница автоматически использует шаблон `index.php` с всеми секциями.

## 📝 Быстрая настройка контента

### Изменить контакты:
Откройте `template-parts/contacts.php` и найдите:
```php
<a href="tel:+79033899090">+7 903 389-90-90</a>
```

### Изменить отзывы:
Откройте `template-parts/reviews.php` и найдите массив `$reviews`

### Изменить программы:
Откройте `template-parts/programs.php` и найдите массив `$programs`

## 🎨 Изменение цветов

Откройте `assets/css/custom.css` и измените CSS переменные:
```css
:root {
    --primary: 347 42% 39%;    /* Ваш цвет */
}
```

## 📸 Замена изображений

1. Замените файлы в `assets/images/`
2. Сохраните те же имена файлов
3. Или обновите пути в PHP файлах

## 🔧 Структура файлов

```
edelweiss-garden/
├── style.css          ← Заголовок темы (обязательно!)
├── functions.php      ← Функции и подключения
├── index.php         ← Главная страница
├── header.php        ← Шапка
├── footer.php        ← Подвал
└── template-parts/   ← Секции страницы
```

## ❓ Проблемы?

**Стили не работают?**
- Проверьте консоль браузера
- Убедитесь, что Tailwind CDN загружается

**Изображения не видны?**
- Проверьте пути в `functions.php`
- Убедитесь, что файлы в `assets/images/`

**Меню не работает?**
- Проверьте консоль браузера на ошибки JS
- Убедитесь, что `mobile-menu.js` подключен

## 📚 Дополнительная информация

- Полная документация: `README.md`
- Инструкция по миграции: `MIGRATION.md`

---

**Версия**: 1.0.0  
**Совместимость**: WordPress 6.0+, PHP 8.1+

