# Локальная разработка без WordPress

Эта папка содержит версию темы, которая работает **без WordPress** для локальной разработки.

## 🚀 Быстрый старт

### 1. Запустите PHP сервер с роутером:

```bash
cd wp-theme/local
php -S localhost:8000 router.php
```

### 2. Откройте в браузере:

```
http://localhost:8000/
```

## 📄 Доступные страницы

- `/` - Главная страница
- `/about/` - О саде
- `/groups/` - Возрастные группы
- `/programs/` - Программы (с подразделами)
- `/pricing/` - Режим и стоимость
- `/adaptation/` - Адаптация
- `/enrollment/` - Как записаться
- `/contacts/` - Контакты

## 📁 Структура

```
local/
├── config.php          # Заменяет WordPress функции + роутинг
├── router.php          # Роутер для многостраничного сайта
├── index.php          # Главная страница
├── about.php          # Страница "О саде"
├── groups.php         # Страница "Возрастные группы"
├── programs.php       # Страница "Программы"
├── pricing.php        # Страница "Режим и стоимость"
├── adaptation.php     # Страница "Адаптация"
├── enrollment.php     # Страница "Как записаться"
├── contacts.php       # Страница "Контакты"
├── header.php         # Шапка
├── footer.php         # Подвал
├── template-parts/    # Секции (hero, about, programs, reviews, contacts)
├── assets/            # CSS, JS, изображения
└── README.md          # Этот файл
```

## 🔧 Как это работает

Файл `config.php` содержит функции-замены для WordPress:
- `get_header()` / `get_footer()` - подключают header.php и footer.php
- `get_template_part()` - подключает файлы из template-parts/
- `edelweiss_image_url()` - возвращает пути к изображениям
- `wp_head()` / `wp_footer()` - подключают стили и скрипты
- `get_current_page()` - определяет текущую страницу
- `get_page_template()` - возвращает путь к шаблону страницы

Файл `router.php` обрабатывает все запросы и направляет их на соответствующие страницы.

Все работает точно так же, как в WordPress, но без установки WordPress!

## ✏️ Редактирование

Редактируйте файлы здесь, и они будут работать локально. Когда будете готовы, изменения можно перенести в WordPress версию в папке `edelweiss-garden/`.

## 🔄 Синхронизация с WordPress версией

Файлы в `template-parts/` и `assets/` идентичны WordPress версии. Можно редактировать здесь, а потом копировать в WordPress тему.

## 📝 Примечания

- Используется PHP встроенный сервер (не требует Apache/Nginx)
- Все пути настроены автоматически
- Работает на любом порту (по умолчанию 8000)

## 🐛 Решение проблем

**Проблема**: Изображения не загружаются
**Решение**: Убедитесь, что запускаете сервер из папки `local/`

**Проблема**: Стили не применяются
**Решение**: Проверьте, что Tailwind CDN доступен (нужен интернет)

**Проблема**: Ошибка 404
**Решение**: Убедитесь, что запускаете сервер с `router.php`: `php -S localhost:8000 router.php`

**Проблема**: Страницы не открываются
**Решение**: Проверьте, что используете правильные пути (например, `/about/` вместо `/about.php`)

