# Локальный просмотр темы

## 🚀 Быстрый способ (HTML предпросмотр)

Я создал файл `preview.html` для быстрого просмотра дизайна без установки WordPress.

### Откройте в браузере:

1. **Просто откройте файл:**
   ```
   Откройте: wp-theme/preview.html
   ```
   
   Или через терминал:
   ```bash
   open wp-theme/preview.html
   ```

2. **Через локальный сервер (рекомендуется):**
   
   Я уже запустил сервер на порту 8000. Откройте в браузере:
   ```
   http://localhost:8000/preview.html
   ```
   
   Если сервер не запущен, запустите:
   ```bash
   cd wp-theme
   python3 -m http.server 8000
   ```
   
   Затем откройте: http://localhost:8000/preview.html

## 📦 Полноценный WordPress локально

Для полного функционала WordPress есть несколько вариантов:

### Вариант 1: Local by Flywheel (самый простой)

1. Скачайте [Local by Flywheel](https://localwp.com/)
2. Установите и запустите
3. Создайте новый сайт
4. Скопируйте тему в `wp-content/themes/edelweiss-garden`
5. Активируйте тему в админке

### Вариант 2: Docker (для разработчиков)

```bash
# Создайте docker-compose.yml
docker-compose up -d

# Скопируйте тему
cp -r edelweiss-garden /path/to/wordpress/wp-content/themes/
```

### Вариант 3: XAMPP/MAMP

1. Установите XAMPP (Mac/Windows) или MAMP (Mac)
2. Установите WordPress в папку `htdocs` или `www`
3. Скопируйте тему в `wp-content/themes/`
4. Откройте `http://localhost/wordpress`

### Вариант 4: PHP встроенный сервер (только для тестирования)

```bash
# Установите WordPress в папку
cd /path/to/wordpress

# Запустите PHP сервер
php -S localhost:8000

# Откройте в браузере
# http://localhost:8000
```

## ⚡ Самый быстрый способ прямо сейчас

Просто откройте файл `preview.html` в браузере - он уже содержит весь дизайн и работает без WordPress!

```bash
# Из корня проекта:
open wp-theme/preview.html

# Или через браузер:
# Файл → Открыть → wp-theme/preview.html
```

## 📝 Примечания

- **preview.html** - упрощенная версия для быстрого просмотра дизайна
- **WordPress тема** - полноценная тема для установки на WordPress
- Для полного функционала нужен WordPress (формы, админка, плагины)

