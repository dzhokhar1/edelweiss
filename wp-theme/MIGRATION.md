# Инструкция по миграции на WordPress

## Что было сделано

✅ Создана полноценная WordPress тема на основе React приложения
✅ Все компоненты конвертированы в PHP шаблоны
✅ Сохранен оригинальный дизайн и функциональность
✅ Использован классический стек: PHP + HTML + Tailwind CSS

## Структура темы

```
wp-theme/edelweiss-garden/
├── style.css              # Заголовок темы (обязательный)
├── functions.php          # Функции и подключения
├── index.php             # Главная страница
├── header.php            # Шапка сайта
├── footer.php            # Подвал сайта
├── template-parts/       # Секции страницы
│   ├── hero.php
│   ├── about.php
│   ├── programs.php
│   ├── reviews.php
│   └── contacts.php
├── assets/
│   ├── css/custom.css    # Кастомные стили
│   ├── js/
│   │   ├── main.js       # Основной JS
│   │   └── mobile-menu.js
│   └── images/           # Все изображения
└── README.md
```

## Установка темы

### Вариант 1: Через админ-панель WordPress

1. Заархивируйте папку `edelweiss-garden` в ZIP
2. WordPress → Внешний вид → Темы → Добавить новую → Загрузить тему
3. Выберите ZIP и установите
4. Активируйте тему

### Вариант 2: Через FTP/SFTP

1. Загрузите папку `edelweiss-garden` в `/wp-content/themes/`
2. WordPress → Внешний вид → Темы → Активировать

### Вариант 3: Через командную строку

```bash
cd /path/to/wordpress/wp-content/themes/
cp -r /path/to/wp-theme/edelweiss-garden ./
```

## Что изменилось по сравнению с React версией

### Технологии

| Было (React) | Стало (WordPress) |
|-------------|-------------------|
| React 18 + TypeScript | PHP 8.1+ |
| Vite | WordPress Theme API |
| React Router | Статическая страница |
| React компоненты | PHP шаблоны |
| Tailwind CSS (локально) | Tailwind CSS (CDN) |
| Lucide React иконки | SVG иконки встроенные |

### Структура

- **React компоненты** → **PHP шаблоны** в `template-parts/`
- **useState hooks** → **Vanilla JavaScript**
- **React Router** → **Якорные ссылки** (#about, #programs и т.д.)
- **Импорты** → **get_template_part()** и **edelweiss_image_url()**

### Функциональность

✅ Все функции сохранены:
- Мобильное меню работает
- Плавная прокрутка по якорям
- Адаптивный дизайн
- Все анимации и переходы

## Настройка контента

### Редактирование текстов

Откройте соответствующий файл в `template-parts/`:

- **Контакты**: `template-parts/contacts.php`
- **Программы**: `template-parts/programs.php`
- **Отзывы**: `template-parts/reviews.php`
- **О нас**: `template-parts/about.php`
- **Hero**: `template-parts/hero.php`

### Изменение изображений

1. Замените файлы в `assets/images/`
2. Или обновите пути в PHP файлах

### Изменение цветов

Отредактируйте CSS переменные в `assets/css/custom.css`:

```css
:root {
    --primary: 347 42% 39%;      /* Основной цвет */
    --secondary: 28 88% 58%;     /* Вторичный цвет */
    --accent: 142 55% 45%;       /* Акцентный цвет */
}
```

## Дальнейшая кастомизация

### Добавление WordPress функций

Вы можете расширить тему, добавив в `functions.php`:

1. **Поддержку виджетов**
2. **Кастомные типы постов** (для программ, отзывов)
3. **Интеграцию с плагинами** (контактные формы, SEO)
4. **Настройки через Customizer**

### Пример: Добавление формы обратной связи

```php
// В functions.php
function edelweiss_garden_contact_form() {
    // Используйте Contact Form 7 или другой плагин
    // Или создайте свою форму
}
```

### Пример: Сделать отзывы редактируемыми через админку

1. Создайте кастомный тип поста "Отзывы"
2. Обновите `template-parts/reviews.php` для загрузки из WordPress
3. Используйте `WP_Query` для вывода отзывов

## Оптимизация производительности

### Рекомендации:

1. **Кэширование**: Используйте плагины кэширования (WP Super Cache, W3 Total Cache)
2. **Оптимизация изображений**: Сжимайте изображения перед загрузкой
3. **Минификация**: Минифицируйте CSS и JS файлы
4. **CDN**: Используйте CDN для статических файлов

### Локальная компиляция Tailwind (опционально)

Если хотите использовать локальную версию Tailwind вместо CDN:

1. Установите Node.js и npm
2. Создайте `package.json`:
```json
{
  "scripts": {
    "build-css": "tailwindcss -i ./assets/css/input.css -o ./assets/css/output.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
```

3. Создайте `tailwind.config.js`
4. Обновите `functions.php` для подключения локального файла

## Проверка после установки

После установки темы проверьте:

- [ ] Тема активирована без ошибок
- [ ] Все секции отображаются корректно
- [ ] Мобильное меню работает
- [ ] Изображения загружаются
- [ ] Якорные ссылки работают
- [ ] Стили применяются правильно
- [ ] Нет ошибок в консоли браузера

## Решение проблем

### Проблема: Стили не применяются

**Решение**: 
- Проверьте, что Tailwind CDN загружается
- Проверьте консоль браузера на ошибки
- Убедитесь, что `custom.css` подключен

### Проблема: Изображения не отображаются

**Решение**:
- Проверьте пути к изображениям в `functions.php`
- Убедитесь, что файлы находятся в `assets/images/`
- Проверьте права доступа к файлам

### Проблема: Мобильное меню не работает

**Решение**:
- Проверьте, что `mobile-menu.js` подключен
- Откройте консоль браузера и проверьте на ошибки JavaScript
- Убедитесь, что jQuery не конфликтует (если используется)

### Проблема: Плавная прокрутка не работает

**Решение**:
- Проверьте, что `main.js` подключен
- Убедитесь, что якорные ссылки имеют формат `#section-id`

## Следующие шаги

1. ✅ Установите тему на WordPress
2. ✅ Проверьте все функции
3. 🔄 Настройте контент под ваши нужды
4. 🔄 Добавьте необходимые плагины (контактные формы, SEO)
5. 🔄 Оптимизируйте производительность
6. 🔄 Настройте резервное копирование

## Поддержка

При возникновении вопросов:
- Проверьте README.md в папке темы
- Изучите код комментарии в PHP файлах
- Проверьте логи WordPress и PHP

## Версия

**Версия темы**: 1.0.0
**Дата создания**: 2025
**Совместимость**: WordPress 6.0+, PHP 8.1+

